{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the primary Battery Dashboard window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
